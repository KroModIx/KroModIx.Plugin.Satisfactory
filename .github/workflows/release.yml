name: Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: write
  packages: read

jobs:
  build-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x
          cache: true
          cache-dependency-path: Directory.Packages.props
      - name: Version ermitteln
        id: ver
        run: |
          if [ "${GITHUB_REF_TYPE}" = "tag" ]; then
            echo "v=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"
          else
            echo "v=0.0.0-dev.${GITHUB_RUN_NUMBER}" >> "$GITHUB_OUTPUT"
          fi
      - name: Restore (GitHub Packages)
        env:
          NUGET_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          dotnet nuget update source kromodix-github \
            --username github-actions \
            --password "${NUGET_AUTH_TOKEN}" \
            --store-password-in-clear-text
          dotnet restore
      - name: Build
        run: >
          dotnet build --no-restore -c Release
          -p:Version=${{ steps.ver.outputs.v }}
      - name: Plugin-Bundle packen (mit plugin.json.version-Patch)
        # WICHTIG: die plugin.json im Source-Repo enthält eine hart codierte
        # "version"-Zeile (z.B. "1.7.0"). Beim Release wird die durch die
        # Tag-Version ersetzt, damit der Host nach dem Update-Install nicht
        # eine Endlosschleife hat („Update verfügbar" → Install → beim
        # nächsten Check wieder „Update verfügbar" weil version im Bundle
        # unverändert ist).
        run: |
          mkdir -p bundle
          # v0.9.1: alle Runtime-DLLs mitkopieren (nicht nur Plugin-DLL),
          # damit transitive Deps wie Markdig im Bundle landen.
          # Host-Provided (Contracts/Avalonia/CommunityToolkit) sind eh
          # nicht in bin/Release, weil sie via ExcludeAssets="runtime"
          # ausgefiltert werden. deps.json ist Pflicht fuer den Loader.
          cp KroModIx.Plugin.Satisfactory/bin/Release/net10.0/*.dll bundle/
          cp KroModIx.Plugin.Satisfactory/bin/Release/net10.0/*.deps.json bundle/
          cp KroModIx.Plugin.Satisfactory/bin/Release/net10.0/plugin.json bundle/
          # Version im Bundle-Manifest auf die Tag-Version setzen (nur wenn Tag-Build).
          if [ "${GITHUB_REF_TYPE}" = "tag" ]; then
            sed -i -E 's/("version"\s*:\s*)"[^"]+"/\1"'"${{ steps.ver.outputs.v }}"'"/' bundle/plugin.json
            echo "== gepatchte plugin.json =="
            grep '"version"' bundle/plugin.json
          fi
          cd bundle
          zip -r "../KroModIx.Plugin.Satisfactory-${{ steps.ver.outputs.v }}.zip" .
      - name: GitHub-Release
        if: github.ref_type == 'tag'
        uses: softprops/action-gh-release@v3
        with:
          files: KroModIx.Plugin.Satisfactory-*.zip
          generate_release_notes: true
          fail_on_unmatched_files: true
